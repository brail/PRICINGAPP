# ===========================================
# PRICING CALCULATOR v0.1 - Produzione
# ===========================================

NODE_ENV=production
PORT=5001
HOST=0.0.0.0

# Configurazione CORS per produzione
# Sostituire con il dominio reale quando disponibile
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:80,http://127.0.0.1:80,http://localhost,http://127.0.0.1

# Per un dominio personalizzato, usare:
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
