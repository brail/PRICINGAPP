# ==================================
# Pricing Calculator v0.3.0
# Server Environment Configuration
# ==================================

# Node Environment
NODE_ENV=development
PORT=5001
HOST=0.0.0.0

# Database
DATABASE_PATH=./data/pricing.db

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# JWT Secrets (CHANGE IN PRODUCTION!)
JWT_SECRET=your-secret-key-here-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-here-change-in-production

# Logging
LOG_LEVEL=info

# ==================================
# Authentication Provider Configuration
# ==================================

# Local Authentication (Admin only)
ENABLE_LOCAL_AUTH=true

# LDAP/Active Directory Authentication
ENABLE_LDAP_AUTH=false

# Google OAuth2 Authentication
ENABLE_GOOGLE_AUTH=false

# ==================================
# LDAP Configuration (if ENABLE_LDAP_AUTH=true)
# ==================================

# LDAP Server URL
LDAP_URL=ldap://your-ad-server:389

# LDAP Bind Credentials (Encrypted - use encrypt-ldap-credentials.js script)
LDAP_BIND_DN_ENCRYPTED=
LDAP_BIND_PASSWORD_ENCRYPTED=

# LDAP Search Configuration
LDAP_SEARCH_BASE=DC=example,DC=com
LDAP_SEARCH_FILTER=(sAMAccountName={{username}})

# LDAP Group Mapping
LDAP_ADMIN_GROUP=CN=Domain Admins,CN=Users,DC=example,DC=com

# LDAP Domain (for email generation)
LDAP_DOMAIN=example.com

# ==================================
# Google OAuth2 Configuration (if ENABLE_GOOGLE_AUTH=true)
# ==================================

# Google Cloud Console Credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Backend URL (for OAuth callback)
BACKEND_URL=http://localhost:5001

# ==================================
# Notes
# ==================================
# 
# 1. LDAP Credentials Security:
#    - Use server/scripts/encrypt-ldap-credentials.js to generate encrypted credentials
#    - Never commit actual credentials to version control
#    - See docs/LDAP_SECURITY.md for best practices
#
# 2. Google OAuth Setup:
#    - Follow docs/GOOGLE_OAUTH_SETUP.md for complete setup instructions
#    - Configure authorized redirect URIs in Google Cloud Console
#
# 3. Production Deployment:
#    - Always change JWT secrets
#    - Use HTTPS in production
#    - Configure proper CORS origins
#    - Enable appropriate authentication providers
