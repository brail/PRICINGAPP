{
  "name": "pricing-calculator",
  "version": "0.1.0",
  "description": "Applicazione per calcolo prezzi con funzionalità bidirezionali",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "dev:network": "concurrently \"npm run server:network\" \"npm run client:network\"",
    "server": "cd server && npm run dev",
    "server:network": "cd server && cp env.network .env && npm run dev",
    "client": "cd client && npm start",
    "client:network": "cd client && cp env.network .env && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "pricing",
    "calculator",
    "multicurrency"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
